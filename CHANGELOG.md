# Release History

All notable changes to StockAI are documented in this file.

## Unreleased — 2026-08-27

### 基本面估值

- 相对估值目标倍数层级改为：可比公司中位数 → 自身近 3 年历史 50 分位 → 行业整体中位数。
- 新增 forward PE / PEG 校准，仅在一致预期增速有意义时生效。
- 周期行业（证券/银行/有色/钢铁等）自动剔除 PE-TTM，优先 PB/PS。
- 亏损公司（EPS-TTM≤0）改为以自身历史 PB 为主锚，同行 PB/PS 仅作参考。
- 新增“高成长龙头”分支（一致预期增速 ≥30%）：以自身历史 p50 为主锚，同行/行业降级为参考，避免 PB/PS 同行倍数把中枢钉得过低。
- 普通公司相对估值聚合从加权中位数改为加权截尾均值，历史权重提高至 0.8、上限提高至 3.0 倍。
- 新增“疑似重组/资产注入中，相对估值不适用”标记：当每股净资产与每股营收相对市值严重偏低时，不给出估值中枢。
- 支持手动配置可比公司（`config/fundamental_peers.yaml`）与 LLM 校验自动同行公司。

### 量化与行情数据

- 新增 10 年月度 OHLCV 抓取与独立缓存，输出 `monthly_history`。
- 月度 LSTM/LightGBM 模型替代固定规则，输出 `1mo` 信号与 `monthly_backtest`；月线不足 60 根时回退到 MACD+MA 规则。
- 扩展日线特征：ATR14、布林带宽/位置、20 日高低位置、成交额量比。
- 数据卫生：训练前剔除有效值占比过低的列，解决周/月线长均线全 0 问题。

### 后端刷新工具

- `python -m main refresh-reports` 改为“只提交、不轮询”：
  - 一次性并行提交所有 run 到 orchestrator；
  - 将 watchlist 对应行指向新 `run_id` 并标记 `queued`；
  - 立即返回 `SUBMITTED ...`，不再等待最终结果。
- 移除 `--timeout` 与 `--poll-interval` 参数。

### 股票门户与图表

- 新增“保存此图表”：按日/周/月保存当前 K 线快照，每个用户每只股票最多保留 20 条。
- 新增“历史图表”下拉与“历史 K线图”对比窗口：
  - 同窗口左右并排显示当前图与历史快照；
  - 对比模式下弹窗放宽至 1600px，左右图表尺寸与上下沿对齐；
  - 支持删除历史快照。
- 图表细节：股票代码后显示股票名称、估值中枢与现价偏离标签移到 K 线外、MACD 与 K 线对齐、整体放大。

### 基础设施

- `RUN_TIMEOUT_SECONDS` 默认值从 600 提高至 1800。
- 量化内部超时从 150s 提高至 900s，并输出可读超时信息。

### 测试

- 扩展估值、工具、量化、后端刷新、门户图表的单元与集成测试；当前全量测试通过。
